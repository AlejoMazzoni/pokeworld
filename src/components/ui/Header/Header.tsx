import styles from "./Header.module.scss";

const Header = () => {
  return (
    <div className={styles.headerContainer}>
      <div>
        <h1>Header</h1>
      </div>
    </div>
  );
};

export default Header;
